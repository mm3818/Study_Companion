# Exclude the SQLite database file
instance/study_materials.db

# Exclude the uploads directory
app/backend/uploads/

# Exclude Node modules (they are rebuilt in the container)
app/frontend/node_modules/

# Exclude frontend build artifacts (if applicable)
app/frontend/build/

# Exclude logs
*.log
